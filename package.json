{
  "name": "starwood-racing-site",
  "version": "1.0.0",
  "description": "Starwood Racing custom storefront, built on Netlify + Stripe",
  "private": true,
  "scripts": {
    "sync-products": "node scripts/sync-products-to-stripe.js",
    "postsync-products": "cp data/products.json data/addons.json public/data/ && echo '✅ Copied synced data into public/data/ — ready to git add/commit/push.'",
    "generate-feed": "node scripts/generate-google-feed.js",
    "generate-meta-feed": "node scripts/generate-meta-feed.js",
    "postgenerate-meta-feed": "cp data/meta-catalog-feed.csv public/data/ && echo '✅ Copied meta-catalog-feed.csv into public/data/ — ready to git add/commit/push.'"
  },
  "dependencies": {
    "stripe": "^16.0.0"
  }
}
